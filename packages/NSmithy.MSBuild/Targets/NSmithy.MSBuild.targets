<Project>
  <!--
    NSmithy.MSBuild.targets — imported in every project that has NSmithy.MSBuild in its
    NuGet dependency tree (directly or via NSmithy.Server / NSmithy.Client).

    Two independent behaviours, both opt-in via properties:

      SmithyPublish=true   Pack smithy model files into the NuGet package and (when
                           SmithyMavenGroupId is also set) produce a Maven JAR.
                           Requires a smithy-build.json at the project root so that
                           IDE tooling (Smithy VS Code / IntelliJ extensions) can
                           resolve traits from Maven dependencies.
                           Also exposes GetSmithyContractItems / GetSmithyBuildFile
                           so that server/client projects can collect models via a
                           <ProjectReference> to this project.

      (code generation)    Runs automatically when a smithy-build.json exists at the
                           project root, or when SmithySource items are present
                           (collected from a contracts ProjectReference).
                           For server/client projects without a local smithy-build.json,
                           one is synthesized in obj/ from the contracts smithy-build.json
                           with sources patched to absolute paths and the csharp-codegen
                           plugin injected.
                           Controlled by SmithyGenerateServer / SmithyGenerateClient.
  -->

  <!-- ================================================================
       Codegen property defaults.
       These mirror the defaults in build/NSmithy.MSBuild.props, but must also be
       set here: server/client projects pull NSmithy.MSBuild *transitively* (via a
       contracts ProjectReference), so they import this buildTransitive targets file
       but NOT the build/ props. The synthesis + codegen path below runs for exactly
       those projects, so the versions it depends on have to be defaulted here too.
       Conditioned on '' so an explicit value (from props or the consuming project)
       always wins.
       ================================================================ -->
  <PropertyGroup>
    <SmithyVersion Condition="'$(SmithyVersion)' == ''">1.71.0</SmithyVersion>
    <SmithyCSharpCodegenVersion Condition="'$(SmithyCSharpCodegenVersion)' == ''"
      >0.0.0-SNAPSHOT</SmithyCSharpCodegenVersion
    >
    <SmithyGenerateDocs Condition="'$(SmithyGenerateDocs)' == ''">false</SmithyGenerateDocs>
    <SmithyOpenApiProtocol Condition="'$(SmithyOpenApiProtocol)' == ''"></SmithyOpenApiProtocol>
  </PropertyGroup>

  <!-- ================================================================
       MSBuild tasks
       $(MSBuildThisFileDirectory) → buildTransitive/ in the NuGet cache;
       the DLL lives in tasks/net10.0/ one level up.
       ================================================================ -->

  <UsingTask
    TaskName="CreateSmithyJar"
    AssemblyFile="$(MSBuildThisFileDirectory)..\tasks\net10.0\NSmithy.MSBuild.dll"
  />

  <UsingTask
    TaskName="SynthesizeSmithyBuildFile"
    AssemblyFile="$(MSBuildThisFileDirectory)..\tasks\net10.0\NSmithy.MSBuild.dll"
  />

  <!-- ================================================================
       P2P support — targets called by NSmithy.MSBuild on ProjectReference
       items via MSBuild task with SkipNonexistentTargets="true".
       Available in every project so that any project can act as a
       contract source when SmithyPublish=true is set.
       ================================================================ -->

  <!--
    Returns the smithy model files for this project.
    Called by _CollectSmithyItemsFromProjectReferences in dependent projects.
  -->
  <Target Name="GetSmithyContractItems" Returns="@(SmithySource)">
    <ItemGroup>
      <SmithySource Include="$(SmithySources)/**/*.smithy" />
    </ItemGroup>
  </Target>

  <!--
    Returns the path to this project's smithy-build.json.
    Only returns a value for contracts projects (SmithyPublish=true).
    Errors if SmithyPublish=true but smithy-build.json is missing — a contracts
    project must have an explicit smithy-build.json so that IDE tooling can
    resolve traits from Maven dependencies.
    Called by _CollectSmithyItemsFromProjectReferences in dependent projects.
  -->
  <Target Name="GetSmithyBuildFile" Returns="@(_SmithyBuildFileToExpose)">
    <Error
      Text="NSmithy: smithy-build.json is required in contracts projects (SmithyPublish=true). Create smithy-build.json at the project root so that IDE tooling can resolve traits from Maven dependencies."
      Condition="'$(SmithyPublish)' == 'true' and !Exists('$(MSBuildProjectDirectory)/smithy-build.json')"
    />
    <ItemGroup Condition="'$(SmithyPublish)' == 'true' and Exists('$(MSBuildProjectDirectory)/smithy-build.json')">
      <_SmithyBuildFileToExpose Include="$(MSBuildProjectDirectory)/smithy-build.json" />
    </ItemGroup>
  </Target>

  <!-- ================================================================
       Pack integration — SmithyPublish=true only
       ================================================================ -->

  <!--
    Embed smithy model files and smithy-build.json into the NuGet package so that
    NuGet consumers can pick them up.
  -->
  <ItemGroup Condition="'$(SmithyPublish)' == 'true'">
    <None
      Include="$(SmithySources)/**/*.smithy"
      Pack="true"
      PackagePath="build/smithy/%(RecursiveDir)%(Filename)%(Extension)"
    />
    <None
      Include="$(MSBuildProjectDirectory)/smithy-build.json"
      Pack="true"
      PackagePath="build/smithy-build.json"
      Condition="Exists('$(MSBuildProjectDirectory)/smithy-build.json')"
    />
  </ItemGroup>

  <!--
    After dotnet pack, create a Maven JAR alongside the NuGet package.
    Gated on SmithyMavenGroupId so projects that only want NuGet distribution
    can skip it by leaving SmithyMavenGroupId unset.
  -->
  <Target
    Name="_CreateSmithyJar"
    AfterTargets="Pack"
    Condition="'$(SmithyPublish)' == 'true' and '$(SmithyMavenGroupId)' != ''"
  >
    <Error
      Text="NSmithy: SmithyMavenArtifactId must be set when SmithyMavenGroupId is set."
      Condition="'$(SmithyMavenArtifactId)' == ''"
    />
    <ItemGroup>
      <_JarSmithySource Include="$(SmithySources)/**/*.smithy" />
    </ItemGroup>
    <CreateSmithyJar
      Sources="@(_JarSmithySource)"
      SourcesRoot="$(SmithySources)"
      GroupId="$(SmithyMavenGroupId)"
      ArtifactId="$(SmithyMavenArtifactId)"
      Version="$(PackageVersion)"
      OutputDirectory="$(PackageOutputAbsolutePath)"
    />
  </Target>

  <!-- ================================================================
       Code generation — runs when smithy-build.json exists or SmithySource
       items are present (from a contracts ProjectReference)
       ================================================================ -->

  <PropertyGroup>
    <SmithyBuildFile Condition="'$(SmithyBuildFile)' == ''"
      >$(MSBuildProjectDirectory)/smithy-build.json</SmithyBuildFile
    >
    <SmithyProjection Condition="'$(SmithyProjection)' == ''">source</SmithyProjection>
    <SmithyPlugin Condition="'$(SmithyPlugin)' == ''">csharp-codegen</SmithyPlugin>
    <SmithyBuildOutputPath Condition="'$(SmithyBuildOutputPath)' == ''"
      >$(IntermediateOutputPath)Smithy/</SmithyBuildOutputPath
    >
    <SmithyGeneratedOutputPath Condition="'$(SmithyGeneratedOutputPath)' == ''"
      >$(SmithyBuildOutputPath)$(SmithyProjection)/$(SmithyPlugin)/</SmithyGeneratedOutputPath
    >
    <SmithyAllProjectionsGlob Condition="'$(SmithyAllProjectionsGlob)' == ''"
      >$(SmithyBuildOutputPath)*/$(SmithyPlugin)/**/*.g.cs</SmithyAllProjectionsGlob
    >
    <SmithyProtoPlugin Condition="'$(SmithyProtoPlugin)' == ''">proto-codegen</SmithyProtoPlugin>
    <SmithyAllProtosGlob Condition="'$(SmithyAllProtosGlob)' == ''"
      >$(SmithyBuildOutputPath)*/$(SmithyProtoPlugin)/**/*.proto</SmithyAllProtosGlob
    >
    <SmithyGrpcServices Condition="'$(SmithyGrpcServices)' == ''">Both</SmithyGrpcServices>
    <SmithyExtraArgs Condition="'$(SmithyExtraArgs)' == ''"></SmithyExtraArgs>
    <SmithyGenerateClient Condition="'$(SmithyGenerateClient)' == ''">true</SmithyGenerateClient>
    <!--
      Opt-in: generate the IHttpClientFactory registration extension
      ({Service}.DependencyInjection.g.cs). Off by default because it requires
      Microsoft.Extensions.Http, which plain (non-DI) clients must not be forced to depend on.
      This flows into the synthesized smithy-build.json as the csharp-codegen
      generateDependencyInjection setting, so the file is only *generated* when enabled — not
      generated-then-excluded. When enabling, also reference Microsoft.Extensions.Http (or the
      Microsoft.AspNetCore.App shared framework, which already provides it). Projects with an
      explicit smithy-build.json set "generateDependencyInjection": true on the csharp-codegen
      plugin instead.
    -->
    <SmithyGenerateDependencyInjection Condition="'$(SmithyGenerateDependencyInjection)' == ''"
      >false</SmithyGenerateDependencyInjection
    >
  </PropertyGroup>

  <!--
    Pre-register the smithy-generated .proto as a <Protobuf> item STATICALLY
    (outside any target body) so that Grpc.Tools' Protobuf_Compile target is
    included in the build graph.

    MSBuild evaluates Protobuf_Compile's Condition="'@(Protobuf)' != ''" at
    graph-build time, before any targets run. Items added inside target bodies
    (like _AddSmithyGeneratedProtoItems) are invisible at that point, so
    Protobuf_Compile would be excluded from the graph and Grpc.Tools would
    never generate C# from the proto file.

    The glob here is identical to what _AddSmithyGeneratedProtoItems adds.
    On the very first build after a clean obj (proto does not exist yet) the
    glob expands to empty, Protobuf_Compile is skipped, and compilation fails.
    Running 'dotnet build' a second time picks up the proto file that the
    first-pass smithy build produced and compilation succeeds.

    This is a known two-pass limitation for gRPC projects with a generated
    proto. Checked-in proto files (e.g. non-smithy setups) do not have this
    restriction.
  -->
  <ItemGroup Condition="'$(SmithyGrpc)' == 'true'">
    <Protobuf
      Include="$(SmithyAllProtosGlob)"
      GrpcServices="$(SmithyGrpcServices)"
      ProtoRoot="$(SmithyBuildOutputPath)"
      Access="Public"
      Visible="$(SmithyEmitGeneratedFiles)"
    />
  </ItemGroup>

  <!-- ================================================================
       Bundled Smithy CLI — select the platform-specific binary that was
       packed into tools/smithy-cli/{platform}/ at NuGet pack time.

       $(SmithyCliPath) may be set by the user to override entirely.
       When not set, we detect OS + architecture, point at the bundled
       binary, and fall back to "smithy" on PATH if the binary is absent
       (e.g. package was packed without running download-smithy-cli.sh).
       ================================================================ -->

  <!-- Detect OS and CPU architecture -->
  <PropertyGroup>
    <_SmithyIsWindows>$([System.Runtime.InteropServices.RuntimeInformation]::IsOSPlatform($([System.Runtime.InteropServices.OSPlatform]::Windows)))</_SmithyIsWindows>
    <_SmithyIsMacOS>$([System.Runtime.InteropServices.RuntimeInformation]::IsOSPlatform($([System.Runtime.InteropServices.OSPlatform]::OSX)))</_SmithyIsMacOS>
    <_SmithyArchStr>$([System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture)</_SmithyArchStr>
  </PropertyGroup>

  <!-- Map OS + arch to the Smithy CLI release platform identifier -->
  <PropertyGroup Condition="'$(_SmithyIsMacOS)' == 'True' and '$(_SmithyArchStr)' == 'Arm64'">
    <_SmithyCliPlatform>darwin-aarch64</_SmithyCliPlatform>
  </PropertyGroup>
  <PropertyGroup Condition="'$(_SmithyIsMacOS)' == 'True' and '$(_SmithyArchStr)' != 'Arm64'">
    <_SmithyCliPlatform>darwin-x86_64</_SmithyCliPlatform>
  </PropertyGroup>
  <PropertyGroup Condition="'$(_SmithyIsWindows)' == 'True'">
    <_SmithyCliPlatform>windows-x64</_SmithyCliPlatform>
  </PropertyGroup>
  <PropertyGroup Condition="'$(_SmithyIsMacOS)' != 'True' and '$(_SmithyIsWindows)' != 'True' and '$(_SmithyArchStr)' == 'Arm64'">
    <_SmithyCliPlatform>linux-aarch64</_SmithyCliPlatform>
  </PropertyGroup>
  <PropertyGroup Condition="'$(_SmithyIsMacOS)' != 'True' and '$(_SmithyIsWindows)' != 'True' and '$(_SmithyArchStr)' != 'Arm64'">
    <_SmithyCliPlatform>linux-x86_64</_SmithyCliPlatform>
  </PropertyGroup>

  <!-- Build the path to the bundled binary -->
  <PropertyGroup>
    <_SmithyBundledCliRoot>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)../tools/smithy-cli'))</_SmithyBundledCliRoot>
    <!-- The CLI is a self-contained distribution; launcher lives under bin/ -->
    <_SmithyBundledCliPath Condition="'$(_SmithyIsWindows)' == 'True'"
      >$(_SmithyBundledCliRoot)/windows-x64/bin/smithy.bat</_SmithyBundledCliPath
    >
    <_SmithyBundledCliPath
      Condition="'$(_SmithyIsWindows)' != 'True' and '$(_SmithyCliPlatform)' != ''"
      >$(_SmithyBundledCliRoot)/$(_SmithyCliPlatform)/bin/smithy</_SmithyBundledCliPath
    >
  </PropertyGroup>

  <!-- Use bundled CLI when SmithyCliPath is not set and the binary exists -->
  <PropertyGroup Condition="'$(SmithyCliPath)' == '' and '$(_SmithyBundledCliPath)' != '' and Exists('$(_SmithyBundledCliPath)')">
    <SmithyCliPath>$(_SmithyBundledCliPath)</SmithyCliPath>
  </PropertyGroup>
  <!-- Fall back to smithy on PATH (pre-bundling behaviour) -->
  <PropertyGroup Condition="'$(SmithyCliPath)' == ''">
    <SmithyCliPath>smithy</SmithyCliPath>
  </PropertyGroup>

  <!-- ================================================================
       Bundled Maven repo (populated by `gradle :bundleMavenRepo`, packed into
       tools/maven-repo/). When present, codegen resolves the plugin + first-party
       trait packages from it via SMITHY_MAVEN_REPOS against an isolated cache —
       Smithy's own offline mechanism. Absent (e.g. falling back to `smithy` on PATH
       in-repo), resolution uses the config's maven block as before.
       ================================================================ -->
  <PropertyGroup>
    <_SmithyBundledMavenRepo>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)../tools/maven-repo'))</_SmithyBundledMavenRepo>
  </PropertyGroup>

  <PropertyGroup>
    <SmithyGenerateServer Condition="'$(SmithyGenerateServer)' == ''">true</SmithyGenerateServer>
    <SmithyEmitGeneratedFiles Condition="'$(SmithyEmitGeneratedFiles)' == ''"
      >false</SmithyEmitGeneratedFiles
    >
    <SmithyStampFile Condition="'$(SmithyStampFile)' == ''"
      >$(SmithyBuildOutputPath)NSmithy.Generated.stamp</SmithyStampFile
    >
    <SmithyBaseNamespace Condition="'$(SmithyBaseNamespace)' == ''"></SmithyBaseNamespace>
  </PropertyGroup>

  <ItemGroup>
    <_SmithyGeneratorInput
      Include="$(MSBuildProjectDirectory)/**/*.smithy"
      Exclude="$(MSBuildProjectDirectory)/obj/**;$(MSBuildProjectDirectory)/bin/**;$(SmithyBuildOutputPath)**"
    />
    <_SmithyGeneratorInput Include="$(SmithyBuildFile)" Condition="Exists('$(SmithyBuildFile)')" />
    <!-- Rebuild when the bundled codegen plugin/traits change (e.g. package upgrade). -->
    <_SmithyGeneratorInput
      Include="$(_SmithyBundledMavenRepo)/**/*.jar"
      Condition="Exists('$(_SmithyBundledMavenRepo)')"
    />
    <Watch Include="$(SmithyBuildFile)" Condition="Exists('$(SmithyBuildFile)')" />
    <Watch Include="@(_SmithyGeneratorInput)" />
  </ItemGroup>

  <!--
    Collect SmithySource items and the contracts smithy-build.json path from
    ProjectReferences that expose GetSmithyContractItems / GetSmithyBuildFile
    (i.e. projects with SmithyPublish=true).
    SkipNonexistentTargets="true" makes the GetSmithyBuildFile call a no-op for
    projects that don't set SmithyPublish=true.
  -->
  <Target
    Name="_CollectSmithyItemsFromProjectReferences"
    BeforeTargets="_PrepareSmithyBuildFile"
    Condition="'@(ProjectReference)' != ''"
  >
    <MSBuild
      Projects="@(ProjectReference)"
      Targets="GetSmithyContractItems"
      Properties="BuildProjectReferences=false"
      SkipNonexistentTargets="true"
      RebaseOutputs="true"
    >
      <Output TaskParameter="TargetOutputs" ItemName="SmithySource" />
    </MSBuild>
    <MSBuild
      Projects="@(ProjectReference)"
      Targets="GetSmithyBuildFile"
      Properties="BuildProjectReferences=false"
      SkipNonexistentTargets="true"
      RebaseOutputs="false"
    >
      <Output TaskParameter="TargetOutputs" ItemName="SmithyContractsBuildFile" />
    </MSBuild>
  </Target>

  <!--
    Synthesize a smithy-build.json when none exists on disk but SmithySource items
    are present (collected from a contracts ProjectReference).
    Reads the contracts smithy-build.json, patches sources to absolute paths, and
    injects the csharp-codegen Maven dependency and plugin entry.
    The condition check for SmithySource must be inside the target body — MSBuild
    evaluates target Condition attributes before DependsOnTargets run, so
    SmithySource would always appear empty at the Condition evaluation point.
  -->
  <Target
    Name="_PrepareSmithyBuildFile"
    BeforeTargets="GenerateSmithyCode"
    DependsOnTargets="_CollectSmithyItemsFromProjectReferences"
    Condition="!Exists('$(SmithyBuildFile)')"
  >
    <Error
      Text="NSmithy: SmithyService must be set when generating from a contracts reference. Add &lt;SmithyService&gt;my.namespace#MyService&lt;/SmithyService&gt; to your project."
      Condition="'@(SmithySource)' != '' and '$(SmithyService)' == ''"
    />
    <Error
      Text="NSmithy: could not find smithy-build.json in the contracts project. Ensure the contracts project has SmithyPublish=true and a smithy-build.json at its root."
      Condition="'@(SmithySource)' != '' and '@(SmithyContractsBuildFile)' == ''"
    />

    <PropertyGroup Condition="'@(SmithySource)' != ''">
      <_SmithySynthesizedBuildFile>$(IntermediateOutputPath)smithy-build.json</_SmithySynthesizedBuildFile>
    </PropertyGroup>

    <MakeDir Directories="$(IntermediateOutputPath)" Condition="'@(SmithySource)' != ''" />

    <SynthesizeSmithyBuildFile
      ContractsBuildFile="@(SmithyContractsBuildFile)"
      Sources="@(SmithySource)"
      Service="$(SmithyService)"
      BaseNamespace="$(SmithyBaseNamespace)"
      CSharpCodegenVersion="$(SmithyCSharpCodegenVersion)"
      SmithyVersion="$(SmithyVersion)"
      GenerateDocs="$(SmithyGenerateDocs)"
      GenerateClient="$(SmithyGenerateClient)"
      GenerateServer="$(SmithyGenerateServer)"
      GenerateDependencyInjection="$(SmithyGenerateDependencyInjection)"
      OpenApiProtocol="$(SmithyOpenApiProtocol)"
      Grpc="$(SmithyGrpc)"
      ProtoPlugin="$(SmithyProtoPlugin)"
      OutputFile="$(_SmithySynthesizedBuildFile)"
      Condition="'@(SmithySource)' != ''"
    />

    <PropertyGroup Condition="'@(SmithySource)' != ''">
      <SmithyBuildFile>$(_SmithySynthesizedBuildFile)</SmithyBuildFile>
    </PropertyGroup>
  </Target>

  <!--
    Ensure the bundled Smithy CLI binary is executable on Unix.
    NuGet does not preserve file permissions, so we chmod +x on first use.
    This is a no-op if the bit is already set, so it's safe to run every build.
  -->
  <Target
    Name="_EnsureSmithyCliExecutable"
    BeforeTargets="GenerateSmithyCode"
    Condition="'$(_SmithyIsWindows)' != 'True' and '$(SmithyCliPath)' != 'smithy' and Exists('$(SmithyCliPath)')"
  >
    <Exec Command="chmod +x &quot;$(SmithyCliPath)&quot;" />
  </Target>

  <Target
    Name="GenerateSmithyCode"
    BeforeTargets="CoreCompile"
    Condition="Exists('$(SmithyBuildFile)')"
    Inputs="@(_SmithyGeneratorInput);@(SmithySource);@(SmithyContractsBuildFile)"
    Outputs="$(SmithyStampFile)"
  >
    <MakeDir Directories="$(SmithyBuildOutputPath)" />
    <!-- Point the CLI at the bundled Maven repo with an isolated cache so codegen
         resolves the plugin + traits from local files. Only when the repo actually
         holds jars (the dir itself is committed just to carry a .gitignore); otherwise
         leave the CLI's default (network) resolution in place. -->
    <ItemGroup>
      <_SmithyBundleJar Include="$(_SmithyBundledMavenRepo)/**/*.jar" />
    </ItemGroup>
    <PropertyGroup Condition="'@(_SmithyBundleJar)' != ''">
      <_SmithyMavenEnv>SMITHY_MAVEN_REPOS=$([System.Uri]::new('$(_SmithyBundledMavenRepo)').AbsoluteUri);SMITHY_MAVEN_CACHE=$([System.IO.Path]::GetFullPath('$(MSBuildProjectDirectory)/$(IntermediateOutputPath)smithy-maven-cache'))</_SmithyMavenEnv>
    </PropertyGroup>
    <PropertyGroup>
      <_SmithyCliBuildOutput>$(SmithyBuildOutputPath.TrimEnd('/').TrimEnd('\'))</_SmithyCliBuildOutput>
    </PropertyGroup>
    <Exec
      Command="&quot;$(SmithyCliPath)&quot; build --config &quot;$(SmithyBuildFile)&quot; --output &quot;$(_SmithyCliBuildOutput)&quot; $(SmithyExtraArgs)"
      EnvironmentVariables="$(_SmithyMavenEnv)"
      IgnoreExitCode="true"
      StandardOutputImportance="High"
      StandardErrorImportance="High"
      WorkingDirectory="$(MSBuildProjectDirectory)"
    >
      <Output TaskParameter="ExitCode" PropertyName="_SmithyCliExitCode" />
    </Exec>
    <Error
      Code="NSMITHYCLI"
      Text="NSmithy: Smithy CLI failed with exit code $(_SmithyCliExitCode). Command: &quot;$(SmithyCliPath)&quot; build --config &quot;$(SmithyBuildFile)&quot; --output &quot;$(_SmithyCliBuildOutput)&quot; $(SmithyExtraArgs)"
      Condition="'$(_SmithyCliExitCode)' != '0'"
    />
    <Touch Files="$(SmithyStampFile)" AlwaysCreate="true" />

    <ItemGroup>
      <FileWrites Include="$(SmithyAllProjectionsGlob)" />
      <FileWrites Include="$(SmithyStampFile)" />
    </ItemGroup>
  </Target>

  <Target
    Name="_AddSmithyGeneratedCompileItems"
    DependsOnTargets="GenerateSmithyCode"
    BeforeTargets="CoreCompile"
    Condition="Exists('$(SmithyBuildFile)')"
  >
    <ItemGroup>
      <Compile Remove="$(SmithyAllProjectionsGlob)" />
      <_SmithyExcludedGeneratedCompile
        Include="$(SmithyBuildOutputPath)*/$(SmithyPlugin)/**/*.Client.g.cs"
        Condition="'$(SmithyGenerateClient)' != 'true'"
      />
      <_SmithyExcludedGeneratedCompile
        Include="$(SmithyBuildOutputPath)*/$(SmithyPlugin)/**/*.Server.g.cs"
        Condition="'$(SmithyGenerateServer)' != 'true'"
      />
      <Compile
        Include="$(SmithyAllProjectionsGlob)"
        Exclude="@(_SmithyExcludedGeneratedCompile)"
        Visible="$(SmithyEmitGeneratedFiles)"
      />
    </ItemGroup>
  </Target>

  <!--
    Register the smithy-generated .proto file as a Grpc.Tools <Protobuf> item.
    Must run right before _Protobuf_Compile_BeforeCsCompile so that Grpc.Tools
    picks up the generated .proto and compiles it to C# in the same build pass.

    We cannot use BeforeTargets="BeforeBuild" here because BeforeBuild runs
    before _PrepareSmithyBuildFile updates SmithyBuildFile; at that early point
    Exists('$(SmithyBuildFile)') is false and the target would be excluded from
    the build graph. Using _Protobuf_Compile_BeforeCsCompile as the anchor
    ensures our DependsOnTargets chain (_PrepareSmithyBuildFile → GenerateSmithyCode)
    runs first, generating the .proto, before Grpc.Tools looks for proto items.
  -->
  <Target
    Name="_AddSmithyGeneratedProtoItems"
    DependsOnTargets="GenerateSmithyCode"
    BeforeTargets="_Protobuf_Compile_BeforeCsCompile"
    Condition="'$(SmithyGrpc)' == 'true'"
  >
    <ItemGroup>
      <Protobuf
        Include="$(SmithyAllProtosGlob)"
        GrpcServices="$(SmithyGrpcServices)"
        ProtoRoot="$(SmithyBuildOutputPath)"
        Access="Public"
        Visible="$(SmithyEmitGeneratedFiles)"
      />
    </ItemGroup>
    <Message Importance="high" Text="NSmithy: registered Protobuf items: @(Protobuf)" />
  </Target>

  <!--
    Copy the smithy-openapi output to wwwroot/openapi.json so that ASP.NET Core's
    static file middleware can serve it at /openapi.json.
    Only runs when SmithyOpenApiProtocol is set.
  -->
  <Target
    Name="_CopyOpenApiToWwwroot"
    AfterTargets="GenerateSmithyCode"
    Condition="'$(SmithyOpenApiProtocol)' != ''"
  >
    <PropertyGroup>
      <_SmithyOutputDir>$([MSBuild]::EnsureTrailingSlash('$(MSBuildProjectDirectory)'))$(SmithyBuildOutputPath)</_SmithyOutputDir>
    </PropertyGroup>
    <ItemGroup>
      <_OpenApiJson Include="$(_SmithyOutputDir)**/openapi/*.json" />
    </ItemGroup>
    <MakeDir Directories="$(MSBuildProjectDirectory)/wwwroot" Condition="'@(_OpenApiJson)' != ''" />
    <Copy
      SourceFiles="@(_OpenApiJson)"
      DestinationFiles="$(MSBuildProjectDirectory)/wwwroot/openapi.json"
      Condition="'@(_OpenApiJson)' != ''"
      OverwriteReadOnlyFiles="true"
      SkipUnchangedFiles="true"
    />
  </Target>

  <!--
    Copy the smithy-docgen Sphinx HTML output to wwwroot/docs/ so that ASP.NET
    Core's static file middleware can serve it at /docs/.
    Only runs when SmithyGenerateDocs is true.
  -->
  <Target
    Name="_CopyDocgenToWwwroot"
    AfterTargets="GenerateSmithyCode"
    Condition="'$(SmithyGenerateDocs)' == 'true'"
  >
    <PropertyGroup>
      <_SmithyDocgenHtmlDir>$([MSBuild]::EnsureTrailingSlash('$(MSBuildProjectDirectory)'))$(SmithyBuildOutputPath)source/docgen/build/html/</_SmithyDocgenHtmlDir>
      <_SmithyDocsDestDir>$(MSBuildProjectDirectory)/wwwroot/docs/</_SmithyDocsDestDir>
    </PropertyGroup>
    <ItemGroup>
      <_DocgenHtmlFiles Include="$(_SmithyDocgenHtmlDir)**/*.*" />
    </ItemGroup>
    <MakeDir Directories="$(_SmithyDocsDestDir)" Condition="'@(_DocgenHtmlFiles)' != ''" />
    <Copy
      SourceFiles="@(_DocgenHtmlFiles)"
      DestinationFiles="@(_DocgenHtmlFiles -> '$(_SmithyDocsDestDir)%(RecursiveDir)%(Filename)%(Extension)')"
      Condition="'@(_DocgenHtmlFiles)' != ''"
      OverwriteReadOnlyFiles="true"
      SkipUnchangedFiles="true"
    />
  </Target>
</Project>
