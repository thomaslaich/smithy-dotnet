---
title: NSmithy
description: Smithy tooling for .NET.
template: splash
hero:
  tagline: Smithy tooling for .NET.
  image:
    html: |
      <img
        src="/smithy-dotnet/brand/nsmithy_logo_1.png"
        alt="NSmithy logo"
        style="max-width: min(26rem, 100%); height: auto;"
      />
  actions:
    - text: Get Started
      link: /smithy-dotnet/getting-started/introduction/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/thomaslaich/smithy-dotnet
      icon: github
      variant: minimal
---

import { Card, CardGrid, Code } from '@astrojs/starlight/components';
import TypeHintCode from '../../components/TypeHintCode.astro';

export const smithyCode = `\$version: "2"
namespace example.hello

use aws.protocols#restJson1

/// A simple greeting service.
@restJson1
service HelloService {
    version: "2024-01-01"
    operations: [SayHello]
}

/// Returns a greeting for the given name.
@readonly @http(method: "GET", uri: "/hello/{name}")
operation SayHello {
    input := {
        /// The name to include in the greeting.
        @required @httpLabel name: String
    }
    output := {
        /// The greeting message, e.g. \`"Hello, World!"\`.
        @required message: String
    }
}`;

export const serverCode = `using Example.Hello; // generated by NSmithy

public class HelloHandler : IHelloServiceHandler
//                          ^? IHelloServiceHandler
{
    public Task<SayHelloOutput> SayHelloAsync(
//              ^? SayHelloOutput
        SayHelloInput input,
        CancellationToken ct = default
    )
//      ^? SayHelloInput
    {
        return Task.FromResult(new SayHelloOutput("Hello, " + input.Name + "!"));
//                                 ^? SayHelloOutput
    }
}

builder.Services.AddHelloServiceHandler<HelloHandler>();
//                                      ^? IHelloServiceHandler
app.MapHelloServiceHttp();`;

export const docsCode = `app.MapSmithyOpenApi(); // Scalar UI   →  /openapi
app.MapSmithyDocs();    // Smithy Docs →  /docs`;

export const clientCode = `using Example.Hello; // generated by NSmithy

IHelloServiceClient client = new HelloServiceClient(transport, options);
//                  ^? IHelloServiceClient
//                               ^? HelloServiceClient

var response = await client.SayHelloAsync(new SayHelloInput("World"));
//  ^? SayHelloOutput
//                          ^? SayHelloAsync
//                                            ^? SayHelloInput
Console.WriteLine(response.Message); // Hello, World!`;


<style>{`
  .how-it-works {
    display: grid;
    grid-template-columns: 2fr 3fr;
    column-gap: 3rem;
    row-gap: 3rem;
    align-items: start;
    margin: 3rem 0;
  }
  @media (max-width: 60rem) {
    .how-it-works { grid-template-columns: 1fr; }
  }
  .how-it-works h3 { margin-top: 0; }
  .how-it-works p { color: var(--sl-color-gray-2); line-height: 1.7; }
`}</style>

<div class="how-it-works">
<div>

### Define your contract

Start by describing your API in [Smithy IDL](https://smithy.io) — operations, input and output shapes, and traits. Smithy is protocol-agnostic: the same model can drive a REST service, a gRPC service, or both at once.

</div>
<Code code={smithyCode} lang="smithy" />

<div>

### Implement server stubs

NSmithy generates a typed handler interface — one method per service operation. You fill in the business logic; routing, serialization, and protocol binding are handled by the generated ASP.NET Core adapter.

</div>
<TypeHintCode code={serverCode} lang="csharp" types={{
  'IHelloServiceHandler': 'interface IHelloServiceHandler\n{\n    Task<SayHelloOutput> SayHelloAsync(\n        SayHelloInput input,\n        CancellationToken ct = default\n    );\n}',
  'SayHelloOutput': 'record SayHelloOutput(string Message)',
  'SayHelloInput': 'record SayHelloInput(string Name)',
}} />

<div>

### Use the generated client

NSmithy generates a typed async client with one method per operation. Inputs and outputs are strongly-typed C# records — no string URLs, no manual JSON, no reflection.

</div>
<TypeHintCode code={clientCode} lang="csharp" types={{
  'IHelloServiceClient': 'interface IHelloServiceClient\n{\n    Task<SayHelloOutput> SayHelloAsync(\n        SayHelloInput input,\n        CancellationToken ct = default\n    );\n}',
  'HelloServiceClient': 'class HelloServiceClient : IHelloServiceClient',
  'SayHelloInput': 'record SayHelloInput(string Name)',
  'SayHelloOutput': 'record SayHelloOutput(string Message)',
  'SayHelloAsync': 'Task<SayHelloOutput> IHelloServiceClient.SayHelloAsync(SayHelloInput input, [CancellationToken ct = default])',
}} />
<div>

### Document your APIs

NSmithy generates an interactive [Scalar](https://scalar.com) OpenAPI explorer
and [Sphinx](https://www.sphinx-doc.org) reference documentation straight from
the Smithy model — no manual spec authoring. Both are built at compile time and
served as static files by ASP.NET Core.

</div>
<div>
<Code code={docsCode} lang="csharp" />
<img
  src="/smithy-dotnet/screenshots/landing-page-scalar-ui.png"
  alt="Scalar interactive API explorer showing the GetForecast endpoint"
  style="border-radius: 0.5rem; border: 1px solid var(--sl-color-gray-5); width: 100%;"
/>
</div>
</div>

<CardGrid>
  <Card title="Cross-ecosystem" icon="translate">
    Smithy is the IDL behind AWS's public APIs. The same contract generates clients in Java, Python, TypeScript, Rust, Swift, and more.
  </Card>
  <Card title="Protocol agnostic" icon="random">
    Smithy is protocol agnostic. NSmithy can generate servers and clients for multiple API protocols — REST, JSON-RPC, gRPC, CBOR — and more through extensibility.
  </Card>
  <Card title="MSBuild integration" icon="setting">
    No separate codegen step. MSBuild invokes `smithy build`, adds generated `.g.cs` files to compilation, and tracks model changes for incremental builds.
  </Card>
  <Card title="Type-safe contracts" icon="approve-check">
    Generated handler interfaces and client methods are strongly typed. No string URLs, no manual serialization, no runtime surprises.
  </Card>
</CardGrid>
