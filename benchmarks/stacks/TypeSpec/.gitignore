node_modules/

# Both are derived artifacts, regenerated by `npm run import` / `npm run generate`
# from the Smithy-emitted OpenAPI document. Committing them would invite editing
# them, which is exactly what must not happen to a generated stack.
tsp/
tsp-output/
